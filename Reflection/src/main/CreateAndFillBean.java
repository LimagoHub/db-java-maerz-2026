package main;

import java.util.Scanner;

public class CreateAndFillBean {
    public Object createBean(String fillQualifiedClassName) {
        try(Scanner scanner = new Scanner(System.in)) {
            // Clazz holen
            // Instanz bauen
            // Methoden finden die mit set beginnen
            // Parameter setzen
            return null;
        } catch(Exception e) {
            e.printStackTrace();
            return null;
        }

    }
}
